extends layout

block content
    div.container
        div.row
            div.col-md-2
            div.col-md-8
                if user.identity == "0"
                    | 老师你走错地方了喔~
                if user.identity == "0"
                    h1.text-center 提交作业，请填写作业信息
                    form(class='form-signin', action='/signup', method='POST')
                        input(type='text', name='title', class='form-control', placeholder='作业名', required, autofocus)
                        input(type='text', name='description', class='form-control', placeholder='描述')
                        button(class='btn btn-lg btn-primary btn-block', type='submit') 提交
                        span.clearfix
            div.col-md-2